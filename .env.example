# 관리자 (Vercel 서버 전용 — 브라우저에 노출 안 됨). 코드에 기본 비밀번호를 넣지 않으므로 반드시 설정.
# ADMIN_PASSWORD=강한비밀번호
# (선택) 여러 개 허용 시 쉼표로 구분
# ADMIN_PASSWORDS=예비비번1,예비비번2

# Supabase (필수 - 프로덕션용)
# https://supabase.com/dashboard → Project Settings → API
# NEXT_PUBLIC_* 는 브라우저 번들에 포함됨(보이는 것이 정상). anon 키는 공개용·RLS로 보호.
# service_role 키는 여기 넣지 말 것(서버 전용, NEXT_PUBLIC_ 금지).
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIs...

# 이메일/카카오 로그인 후 돌아올 사이트 주소 (메일 링크가 localhost 가 되면 ERR_CONNECTION_REFUSED 남)
# 운영/Vercel 에는 반드시 실제 배포 URL 로 설정. Supabase 대시보드 Site URL 과도 맞출 것.
# NEXT_PUBLIC_SITE_URL=https://mimisalon.vercel.app
# 로컬 개발만: 메일/카카오 콜백을 localhost 로 받을 때 (npm run dev = 포트 5006)
# NEXT_PUBLIC_USE_LOCAL_AUTH_CALLBACK=1
# (선택) 포트를 바꿔 쓰면 예: http://localhost:3000 — 기본은 이 레포는 5006
# NEXT_PUBLIC_LOCAL_DEV_ORIGIN=http://localhost:5006

# 로컬 개발용 (프로덕션/Vercel에는 절대 설정하지 말 것)
# NEXT_PUBLIC_SKIP_CUSTOMER_AUTH=true  # 로컬: 고객 로그인 생략, 관리자 비밀번호만 필요 (데모 없음)
# NEXT_PUBLIC_SKIP_SUPABASE=1         # Supabase 비활성화 시 localStorage만 사용
# NEXT_PUBLIC_LOCAL_AUTOBACKUP=0      # 로컬 개발 시 IndexedDB 자동 백업 끄기 (기본: dev에서 켜짐)
# NEXT_PUBLIC_HIDE_ADMIN_LINK=1      # 프로덕션 푸터에서 「관리자」 링크 숨김 (기본은 표시)
# NEXT_PUBLIC_ALLOW_ADMIN_SETUP=1    # 운영에서 최초 관리자 비밀번호 설정 허용 (1회 설정 후 0 또는 삭제 권장)
# NEXT_PUBLIC_DISABLE_ADMIN=1          # (선택) 운영에서 /admin 을 완전히 막을 때만. 기본은 설정하지 않음. 켜 두면 관리자 사용 불가 → Vercel에서 삭제 후 재배포. 로컬 npm run dev 는 영향 없음

# 지도 기반 거리 계산 (하나라도 설정 시 지도 API 사용, 없으면 주소 추정)
# Kakao: https://developers.kakao.com/ → 앱 생성 → REST API 키
# KAKAO_REST_API_KEY=your_kakao_rest_api_key
# Naver: https://console.ncloud.com/ → Maps Geocoding
# NAVER_MAPS_CLIENT_ID=your_naver_client_id
# NAVER_MAPS_CLIENT_SECRET=your_naver_client_secret
# Google: https://console.cloud.google.com/ → Geocoding API 활성화
# GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# 예약확정 SMS 발송 (알리고): https://smartsms.aligo.in/
# ALIGO_USER_ID=your_aligo_user_id
# ALIGO_API_KEY=your_aligo_api_key
# ALIGO_SENDER=발신번호
# ALIGO_TESTMODE=Y  # 연동 테스트 시 (실제 발송 없음)
# SKIP_SMS_SEND=1   # 개발 시 SMS 발송 생략 (테스트용)
